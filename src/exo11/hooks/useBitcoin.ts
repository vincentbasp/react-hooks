/**
 * Create a custom hook to regularly fetch the price of Bitcoin
 * Every minute, the price should be re-fetched and the returned variable should be updated. 
 * To retrieve the price of Bitcoin in US dollars, issue a GET request to the following URL: 
 * https://api.coindesk.com/v1/bpi/currentprice.json. 
 * You can find an example of json returned in mocks folder
 * @return { number } the value of Bitcoin in US dollars
 */
export const useBitcoin = () => {
    return;
  };